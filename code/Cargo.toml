[package]
name = "fluid_sim"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
minifb = { version = "0.28.0", optional = true }
rand = "0.10.1"

[features]
default = ["window"]
window = ["dep:minifb"]
